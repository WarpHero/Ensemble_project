strategy: "weighted_fusion"  # ["weighted_fusion", "nms_fusion", "soft_nms"]
weights:
  detection: [0.4, 0.6]  # YOLO, RCNN 가중치
  classification: [0.3, 0.7]
fusion:
  iou_threshold: 0.5
  score_threshold: 0.3
  soft_nms_sigma: 0.5  # Soft-NMS 사용 시
feature_fusion:
  enabled: false
  method: "concat"  # ["concat", "sum", "attention"]