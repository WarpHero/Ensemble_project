# YOLOv8s & Faster R-CNN Ensemble Object Detection

**YOLOv8s**와 **Faster R-CNN**을 앙상블하여 객체 탐지 성능을 향상시키기 위한 모델.
현재(v1.0 기준) **backbone** YOLOv8s의 backbone은 vgg16으로 변경 가능.
colab에서 사용 가능.

## 주요 기능

- **YOLOv8s**와 **Faster R-CNN**의 앙상블:
  
- **Backbone 변경 가능**:
  - 지원되는 backbone:
    - VGG
    - ResNet(예정)
    - EfficientNet(예정)

- **Neck** 개발 중:
  - 현재 **Neck** 부분은 개발 중.
